# Nginx config for Questionnaire under games.wedoeffects.co.uk/questionnaire
# Add this location block to your games.wedoeffects.co.uk server config.
# IMPORTANT: No trailing slash on proxy_pass - keeps full path /questionnaire/play etc.

    location /questionnaire {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
